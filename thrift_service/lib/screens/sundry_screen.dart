import 'package:flutter/material.dart';

class SundryScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.green[100],
    );
  }
}
